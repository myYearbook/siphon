Logging:
    filename: log.txt
    format: "%(levelname) -10s %(asctime)s %(message)s"
    # Valid values: debug, info, warning, error, critical
    level: debug
    
Siphon:
    stomp:
        queues: ['/queue/email.hotmail','/queue/email.yahoo','/queue/email.aol']
        brokers: ['mq01:61613','mq02:61613','mq03:61613','mq04:61613','mq09:61613','mq10:61613']
        username: None
        password: None
        threads_per_broker: 1
    amqp:
        # Queues, Exchanges and Routing Keys are auto-generated by the stomp queues, all direct
        default_exchange: siphon
        compress: True
        compress_level: 9
        delivery_mode: 0
        connection:
            host: mq07
            port: 5672
            user: guest
            pass: guest
            ssl: False
            vhost: /